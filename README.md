# Semantic UI Parser RFC

Reference implementation for a DOM-first Angular/TypeScript parser designed for Taiga UI and other component libraries.

## Run

```bash
npm install
npm test -- --watch=false --browsers=ChromeHeadless
npm run build
npm start
```
