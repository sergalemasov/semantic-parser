# Workspace notes

- Keep parser contracts framework-agnostic TypeScript where possible.
- Preserve semantic nesting and treat registered controls as explicit atoms.
- Validate focused tests and `npm run build` after parser changes.
